# Chunking

Your program must implement different chunking strategies for the different types of files.
• Python code chunking
• Text chunking

The **maximum chunk size is 2000 characters** and it has to be configurable through a CLI argument.

Plan: implement one simple chunker myself, then replace it with a library.
